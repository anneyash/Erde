<%= form_with(model: step_question, local: true) do |form| %>
  <% if step_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(step_question.errors.count, "error") %> prohibited this step_question from being saved:</h2>

      <ul>
      <% step_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :step_id %>
    <%= form.number_field :step_id %>
  </div>

  <div class="field">
    <%= form.label :question_id %>
    <%= form.number_field :question_id %>
  </div>

  <div class="field">
    <%= form.label :position %>
    <%= form.number_field :position %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
