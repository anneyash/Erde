<%= form_with(model: visa_application_form, local: true) do |form| %>
  <% if visa_application_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(visa_application_form.errors.count, "error") %> prohibited this visa_application_form from being saved:</h2>

      <ul>
      <% visa_application_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :country_id %>
    <%= form.number_field :country_id %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
